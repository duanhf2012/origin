# Origin v3.0 维护文档

本目录保存 v3.0 发布后的补丁、兼容性调整、运维变更和补充验证；它们不改写 `../../baseline/v3.0/` 的发布基线事实。

首次产生内容时按需建立：`proposals/`、`design/`、`plans/`、`changes/`、`reports/`、`guides/`。每篇文档必须注明状态、目标版本、兼容性和基线 `v3.0`。
