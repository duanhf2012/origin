# 教程共享代码

这里存放多个教程示例共同使用的合约和少量辅助 Service，不是独立学习入口。

- `tutorialrpc`：只存放 RPC 合约及对应生成代码。
- `tutorialwatcher`：服务发现监听 Service。

RPC 业务实现放在各自示例目录；共享目录只保留合约，避免让生成声明和业务 Service
看起来像同一个对象。

请从上层编号教程目录进入相应示例。
