# 新增一个业务 Service

定义一个匿名嵌入 `service.Service` 的 Go 类型，再用 `app.Setup` 登记；最后在 YAML 的 `services` 中声明该类型名。

```text
run.bat
```

对应教程：[Service 与 Module](../../../docs/baseline/v3.0/guides/03-service-and-module.md)。
