# Origin v3 开发指导原则

## 适用范围

本文件适用于 Origin v3 仓库中的全部设计、实现、重构、测试、评审和文档工作。后续经开发者确认的原则继续追加到本文件，并作为后续讨论和开发工作的共同约束。

## 原则

### 1. 保持代码精简、清晰和可维护

代码应尽量精简、清晰、易于阅读和维护，避免不必要的抽象、层级、依赖和技巧性实现。

当可读性、可维护性与性能、兼容性、开发效率或其他目标发生冲突时，不自行决定取舍。应先列出可选方案、影响和依据，与开发者确认后再实施。涉及性能取舍时，应尽可能使用基准测试或其他可验证的数据作为判断依据。

### 2. 在不增加复杂度的前提下关注 Go 性能

Go 代码在不增加实现复杂度、不降低可读性和可维护性的前提下，应尽量采用性能更好的写法。尤其关注 RPC、序列化、调度、定时器和队列等高频路径中的逃逸分析、堆分配、临时对象、接口装箱、闭包捕获及不必要的数据复制，尽可能减少垃圾回收压力。

性能优化必须针对明确的热点，并尽可能通过逃逸分析、Benchmark 和分配统计进行验证，不为了未经验证的微小收益引入对象池、复杂缓存、unsafe 或难以理解的技巧性实现。性能优化与代码简洁性发生明显冲突时，遵循原则 1，先向开发者说明方案、收益、成本和验证数据，再确认取舍。

### 3. 相对独立的系统分别设计和记录

不同且相对独立的系统应分别讨论，并分别建立设计文档。单个设计文档应聚焦一个系统或一个边界清晰的主题，避免混入过多其他模块的内部设计。

当一个设计需要与其他系统协作时，只在当前文档中说明必要的接口、数据流、依赖和边界，不重复展开其他系统的完整实现。跨系统主题如果包含多个可独立决策的部分，应拆分成多份设计文档，并通过相互引用保持一致。

里程碑设计文件统一命名为 `M{实际编号}-中文主题设计.md`，例如 `M0-工程基础里程碑设计.md`、`M5-TCP网络基础库设计.md`。文件名前缀必须与文档中的真实里程碑编号一致，不使用 `01`、`02` 等人工排序编号代替 M 编号。路线图、复核清单和设计储备等非里程碑文档不添加可能与 M 编号混淆的数字前缀。

### 4. 设计与里程碑是实施门禁

只实现当前里程碑明确列出的范围。里程碑设计文档复核清单未记录“允许实施”时，不得编写该里程碑的实施计划和代码。

实现中发现设计缺口、现有方案无法落地或需要改变已经确认的方案时，应先停止相关实现，更新对应设计文档并与开发者确认后再继续。不得把临时决定只写进代码，也不得顺手实现后续里程碑能力。

### 5. 默认不为未来需求增加抽象

只为当前已经确认的真实需求建立接口、工厂、适配层、扩展点和配置项。类型、函数和字段默认保持内部可见，只有业务或其他已确认组件确实需要使用时才公开。

接口应保持小而明确，优先由使用方定义。不为了“以后可能替换”提前增加层级，也不为了假设中的复用场景建立通用框架。后续出现真实需求时再基于现有代码重构。

### 6. 禁止包级可变全局状态

Application、Node、Service、Logger、BufferPool、Transport、TimerEngine 和其他运行时状态必须由明确的实例持有。包级只允许常量、只读构建信息和不会形成跨实例污染的不可变数据。

同一进程必须能够创建多个相互独立的 Application，测试之间不得通过隐藏的包级状态相互影响。Origin 框架包不得使用 `init()` 隐式注册 Service、修改运行时注册表或启动 goroutine。

最终可执行程序的 `main` 包允许保存唯一的 `Application` 实例，并允许不同的 `init_*.go` 文件在 `func init()` 中对该明确实例调用 `app.Setup(...)`。这只是把服务类型安装到当前实例，不得写入 Origin 包级目录、读取配置、创建运行实例或启动 goroutine；安装顺序也不得影响 Service 的实际启停顺序。单元测试仍应直接创建各自独立的 Application，不复用可执行程序的包级实例。

### 7. 所有资源必须有明确所有者

每个 goroutine 必须明确由哪个组件创建、如何取消、何时退出以及由谁等待。禁止创建无法停止和等待的 fire-and-forget goroutine。

Buffer、连接、Timer、文件、订阅、队列项和 pending 对象必须具有唯一且可追踪的释放责任。`Close`、`Stop` 和 `Cancel` 应尽量保持幂等。启动过程中任何一步失败，都必须按已经成功创建资源的逆序完成清理。

### 8. 队列、并发和重试必须有界

不得创建无限队列、无限 goroutine 或无限 pending；除下述已确认的基础设施恢复特例外，
也不得进行无限重试。队列满、任务过载、连接失败和资源达到上限时必须返回明确错误，不得
静默丢失关键工作。

所有重试必须具有次数或时间上限、明确退避、Context 和退出条件。业务代码不得绕过 Service 调度器并发访问非并发安全的 Service 状态。

RPC Transport 和 Discovery Provider 的运行期持续恢复属于已经确认的基础设施特例：只要
Application 仍在运行且尚未收到 Stop，可以不设置固定尝试次数，但必须由单一所有者执行，
使用有上限且带抖动的退避、可由生命周期 Context 立即取消，并且不得在恢复期间无限累积
goroutine、连接、订阅、pending、消息或待重放请求。这里的“持续”只延长恢复时间，不放宽
任何资源上限；启动期仍受 Start Context 和可选 StartTimeout 限制，确定性配置、认证和权限
错误必须立即失败。

### 9. 测试与实现同时完成

每项实现必须同时补充与风险相匹配的验证：

- 普通逻辑提供单元测试；
- 并发和生命周期代码执行 `go test -race`；
- 资源组件覆盖重复关闭、部分初始化失败、逆序回滚和泄漏测试；
- 性能路径保存 Benchmark、`allocs/op` 和 `B/op`；
- TCP、NATS 和其他协议使用真实协议集成测试；
- 修复缺陷时增加能够稳定复现该缺陷的测试。

没有完成对应测试和验收记录的功能，不视为里程碑完成。

### 10. 库代码不直接控制进程

库代码禁止调用 `os.Exit`、`log.Fatal` 或其他绕过调用方清理流程的退出函数。可预期失败必须通过 error 返回；`panic` 只用于真正违反框架内部不变量、无法安全继续执行的情况。

只有最终可执行程序入口可以在所有清理完成后决定退出码。Origin v3 的无参数
`Application.Start()` 被明确设计为最终程序入口，因此它是唯一允许在内部可测试执行函数
已经返回、全部 Application 资源已经清理、最终错误日志已经刷新后调用 `os.Exit` 的框架
方法。其他 Application 方法和所有其他库代码仍然禁止直接退出进程。错误应保留定位问题所需
的上下文，但不得包含密码、token、凭证或其他敏感业务数据。

### 11. 严格管理第三方依赖

只增加当前里程碑明确需要的第三方依赖，不一次性复制 Origin v2 的依赖集合。引入依赖前应检查维护状态、许可证、依赖规模、性能特征、资源退出能力和可替代方案。

能够用清晰、可靠的标准库代码完成时优先使用标准库，但不为了追求零依赖而重复实现复杂且成熟的基础组件。基础依赖的新增或替换应先向开发者说明收益、成本和替代方案并取得确认。

### 12. 保证构建和生成结果可重复

生成代码不得手工修改。代码生成工具、Protobuf 和其他影响生成结果的工具版本必须固定；相同输入重复生成必须得到相同结果，CI 应检查生成代码是否过期。

所有 Go 代码在交付前必须通过 `gofmt`、对应单元测试、竞态检测和本里程碑规定的静态检查。性能相关代码还必须保存可重复执行的 Benchmark 方法和基线结果。

### 13. 保持测试与示例目录职责清晰

普通单元测试、包级黑盒测试和简单 API 示例应与被测源码放在同一包目录，分别使用 `*_test.go` 和 Go `Example`；不得为可以直接在测试函数中验证的简单逻辑随意创建 `main` 包。

`testdata` 默认只保存当前包测试使用的输入、配置、协议样本和黄金文件，不放置普通测试程序。只有确实必须启动独立进程才能验证的黑盒场景，才允许在 `tests/helpers/<用途>` 中建立测试辅助程序，并必须由对应测试负责构建、启动、停止和清理。

跨多个包的集成测试统一放入 `tests/integration`，完整进程或集群级端到端测试统一放入 `tests/e2e`。面向使用者、可以独立运行的示例统一放入 `examples/<场景名>`，每个示例必须说明用途和运行方式，并跟随根 module 参与 `go build ./...`。

生产可执行程序只放入 `cmd/<程序名>`。除 `cmd`、面向使用者的 `examples` 和经过明确论证的独立进程测试辅助程序外，不在其他目录随意增加 `main` 包。不预先创建没有实际内容的测试或示例目录。

### 14. 每个里程碑完成后提交

每个里程碑只有在实现、测试、Benchmark、文档回写和验收完成后才提交。提交应只包含该里程碑及必要的前置修正，不混入后续里程碑；提交前必须执行里程碑规定的质量检查，并确认工作树中的变更范围清楚。

提交信息使用清晰的中文主题并包含里程碑编号。若发现前置里程碑存在尚未提交的基线，应明确说明并先形成可构建检查点，之后不得再次累积多个已经完成的里程碑而不提交。

### 15. 使用详细中文注释完整说明代码意图和执行步骤

所有新建、修改以及已经存在的 Origin 代码都必须按照本规则检查并补充清晰、准确、足够详细的中文注释，便于开发者不依赖提交历史即可理解实现、维护代码和排查缺陷。公开类型、函数、方法、常量和变量必须具有中文 GoDoc；内部类型、内部函数、关键字段和重要局部状态同样必须说明职责。

每个函数内部应按执行顺序为各个独立逻辑步骤添加中文注释，例如：

1. 参数和状态校验；
2. 默认值、临时对象或资源初始化；
3. 核心处理阶段及其先后顺序；
4. 分支成立的原因和不同分支的行为；
5. 所有权转移、并发同步、错误回滚和资源释放；
6. 最终状态提交或返回结果。

短小函数可以用一条注释概括完整步骤；包含多个阶段、循环、状态切换或错误路径的函数必须分段注释，不能只在函数声明处写一句笼统说明。测试和 Benchmark 也要说明样本构造、关键动作和断言目标，避免后来维护者误删看似多余但实际用于覆盖边界的步骤。

并发调度、状态转换、资源所有权与转移、释放顺序、错误恢复、边界条件、算法选择、性能优化和非显而易见的实现必须进一步说明“为什么这样实现”、成立条件以及修改时不能违反的不变量。注释不能只把代码逐句翻译成中文，也不能为了满足数量要求加入没有信息量的注释。

注释必须随着代码同步维护。修改行为时必须同时检查函数 GoDoc、步骤注释、字段注释和测试说明，禁止保留已经失效、与实现不符或可能误导排障的内容。生成代码和第三方代码不要求修改；语义完全清晰的单行访问器、常量映射和重复断言可以由紧邻的整体注释统一说明，不要求机械地逐行注释。

### 16. 配置中的时间统一使用带单位字符串

所有面向使用者的 JSON/YAML 时间长度配置必须使用带单位字符串，不使用无单位整数，也不把单位编码到字段名中。统一支持 `ns`、`us`、`ms`、`s`、`m`、`h`、`d`，例如 `500us`、`15s`、`2h30m`、`14d`；`d` 固定表示 `24h`，不表示受时区或夏令时影响的自然日。

时间单位区分大小写，组合值必须从大单位到小单位排列。配置值只接受非负整数片段，不接受小数、空单位、重复单位、`day`、`w` 或 `y`；零值写成 `0s`。自然日、日期、时区和每日固定时刻属于日历语义，应使用独立字段表达，不得混用固定 Duration。

Go 内部可以转换并保存为 `time.Duration`。对外配置字段应使用 `timeout`、`interval`、`ttl`、`max_age` 等名称，不再使用 `timeout_ms`、`ttl_seconds`、`max_age_days` 等带单位字段名。

### 17. 配置中的字节大小统一使用简短单位字符串

所有面向使用者的 JSON/YAML 字节大小配置必须使用带单位字符串，统一只支持 `B`、`KB`、`M`、`G`、`T`，例如 `16B`、`64KB`、`4M`、`2G`。这些单位在 Origin 中全部采用二进制倍数：`KB = 1024B`、`M = 1024KB`、`G = 1024M`、`T = 1024G`。

字节单位区分大小写；配置值只接受非负整数和一个单位，不接受小数或复合单位，零值写成 `0B`。不接受裸整数、`byte`、`K`、`MB`、`GB`、`TB`、`KiB`、`MiB`、`GiB` 或 `TiB`，避免同一含义出现多套写法。解析时必须检查语法、乘法和目标类型溢出。

Go 内部统一换算为 Byte 保存和计算。对外配置字段使用 `max_payload_size`、`buffer_size`、`max_file_size` 等名称，不再使用 `max_payload_bytes`、`size_mb` 等带单位字段名。

配置字段名称必须准确表达被限制的对象、数据方向和作用范围。不同模块中语义完全相同的
字段应使用相同名称；语义或方向不同的字段不得为了外观统一而复用同一个模糊名称。例如，
`rpc.max_payload_size` 统一表示单个 RPC 业务 payload 上限；
`tcp.send_queue_messages` 表示 TCP 出站等待发送消息数；
`nats.receive_queue_messages` 表示 NATS 入站等待回调消息数。后两者方向不同，不得合并为
无法判断收发方向的 `queue_messages`。

### 18. 环境变量只替换配置值

JSON/YAML 配置支持 `${NAME}` 环境变量占位符，但只在文件解析后的字符串 Scalar 值中展开，不替换字段名、注释或配置结构，也不对整份原始文本执行 `os.ExpandEnv`。

变量名必须匹配 `[A-Za-z_][A-Za-z0-9_]*`。未定义变量必须返回包含变量名和来源位置的配置错误，不得静默替换为空字符串；已经定义为空字符串的变量允许展开为空值，再由目标类型和业务校验决定是否合法。`$NAME`、默认值表达式、嵌套变量、命令替换和脚本执行均不支持；`$${NAME}` 表示字面量 `${NAME}`。

环境变量值不得出现在日志和错误中。完整 Scalar 只有一个占位符时，允许按目标基础类型、`Duration`、`ByteSize` 或文本解码接口进行严格转换；占位符与其他文本组合时结果只作为字符串处理。环境变量不能生成 Mapping、Sequence 或字段名。

### 19. 配置结构体默认不要求 Tag

Origin 的 JSON/YAML 配置结构体默认不要求 Tag。未声明 Tag 的可导出字段按确定性的 Go 字段名转 `snake_case` 规则匹配，例如 `DefaultTimeout` 对应 `default_timeout`、`NodeID` 对应 `node_id`、`HTTPServerURL` 对应 `http_server_url`。

只有字段需要改名或忽略时才使用标准 `json` Tag，例如 `json:"timeout"` 或 `json:"-"`。配置加载只把 `json` Tag 的名称部分作为覆盖名称，`omitempty` 等编码选项不改变配置解码语义。Origin 不定义 `config` Tag，也不把 `yaml` Tag 作为配置加载契约；新建 Origin 配置结构体和示例不得为了普通字段机械添加 Tag。

配置 Key 与最终映射名称精确匹配。多个字段、匿名嵌入或 Tag 产生相同名称时必须返回配置模型冲突错误，不得按声明顺序静默选择。不能自然表达或容易产生歧义的字段名应显式使用 `json` Tag。

### 20. 持续执行完整测试并尽量覆盖所有可达代码路径

所有实现都必须在开发过程中反复执行与当前改动范围相匹配的完整测试，不能只在里程碑
结束时运行一次成功路径。每次新增或修改逻辑后，应先执行相关包测试，再定期执行全仓
测试；提交前必须重新执行本里程碑规定的全量单元测试、集成测试、竞态检测、静态检查、
跨平台构建及其他适用门禁。

测试设计应以代码路径和系统风险为依据，尽量覆盖：

1. 正常路径、空值、零值、长度边界、非法输入和溢出；
2. 每个可稳定触发的错误分支、部分初始化失败和逆序回滚；
3. Context 取消、Deadline、重试终止、队列满和资源上限；
4. 并发竞争、重复调用、幂等关闭、异常退出和 goroutine 回收；
5. panic 恢复、错误码映射、所有权转移和失败后的对象状态；
6. Windows、Linux、macOS 等受支持平台的差异分支；
7. 修复缺陷前能够复现问题、修复后能够防止回归的专门用例。

实现完成后必须生成逐函数或逐分支覆盖率报告，检查低覆盖函数和未执行路径，并补充能够
稳定、清晰验证真实行为的测试。覆盖率数值不是唯一目标，禁止为了追求百分比编写没有
有效断言的测试、复制实现逻辑到测试中，或为极难发生的系统错误引入复杂生产抽象。
确实无法稳定触发的平台或操作系统故障分支，应记录原因、依赖的集成验证和剩余风险。

“测试通过”表示当前验证范围内没有已知缺陷，不代表数学意义上的绝对无 Bug。发现测试
不稳定、竞态、资源泄漏或未解释失败时，必须先定位并修复，不能通过跳过、放宽断言或
降低测试次数使门禁表面通过。

### 21. 可复用的基础算法统一沉淀到内部公共包

实现业务框架功能时，如果其中包含职责独立、语义通用且可以由其他模块直接复用的基础
算法或数据结构，应从具体模块中提炼到统一的内部公共包，避免在 Service、RPC、Timer、
Transport 等目录中分别维护不同版本。例如，可增长环形队列统一放入
`internal/container/ringqueue`，包名使用能够直接表达数据结构或算法含义的标准英文名称。

内部公共算法包必须遵守以下规则：

1. 默认放在 `internal` 下，不因为“可能有用”就成为面向业务项目的公开兼容 API；
2. 包只实现算法本身，不依赖 Application、Node、Service、RPC、日志或具体业务类型；
3. API 保持最小，只提供当前真实使用场景需要的构造、读写、查询和清理能力；
4. 并发控制由所有权清晰的一层负责；算法包默认不同时内置锁、Channel、日志和指标；
5. 泛型只用于消除真实存在的重复实现，不建立复杂的容器框架、继承层级或适配器体系；
6. 必须独立测试空值、扩容、回绕、边界、引用清理和溢出，并保存必要的 Benchmark 与
   分配结果；
7. 已有模块内的专用实现只有在语义一致、替换能减少重复且不会扩大当前里程碑范围时才
   迁移，不能为了形式统一重写已经稳定且需求不同的算法。

“可以提炼”不等于“所有短小辅助函数都必须抽包”。只被一个模块使用、强依赖该模块状态
或提炼后反而增加参数、接口和理解成本的逻辑，应继续留在所属模块。算法复用与代码简洁性
发生冲突时，仍按原则 1 先说明收益、成本和验证依据，再确认取舍。

### 22. 所有设计必须考虑性能和低延迟

Origin 是面向游戏服务器的低延迟框架。所有功能、模块和里程碑在设计阶段就必须评估性能
与延迟，不能等实现完成后才补做优化。设计文档必须结合当前功能的真实数据流，明确区分
启动、注册、配置和代码生成等冷路径，以及 RPC、序列化、调度、Timer、事件、队列和网络
收发等运行热路径；冷路径可以优先保证清晰和正确，热路径必须避免可预见的不必要开销。

对热路径的设计至少应检查：

1. 是否存在运行时反射、字符串方法查找、重复解析、接口装箱或不必要的动态分派；
2. 是否产生可避免的堆分配、临时对象、闭包捕获、辅助 goroutine 或独立 Runtime Timer；
3. Buffer、消息和参数是否发生不必要的复制，所有权能否在保持安全清晰的前提下转移；
4. 是否增加锁竞争、原子操作、Channel/队列跳转、上下文切换或跨 goroutine 调度次数；
5. 队列、批处理、缓存、对象池和重试是否有界，过载时能否快速失败并保持延迟可控；
6. 序列化格式、协议头、字段布局和系统调用数量是否适合大量小消息与峰值流量；
7. 慢任务、阻塞 I/O、日志、监控和错误处理是否可能拖慢 Service 工作协程或网络协程；
8. 停止、取消、超时和异常路径是否可能形成延迟尖峰、资源滞留或集中唤醒风暴。

性能设计不能只关注平均吞吐量。实施计划和验收应根据功能风险记录适用的
`ns/op`、`allocs/op`、`B/op`、吞吐量以及 P50、P95、P99 等延迟分位数，并覆盖普通负载、
峰值积压、容量上限和过载拒绝场景。涉及网络和 RPC 时应使用具有代表性的小消息、普通
消息和允许的最大消息进行基准或集成验证；涉及并发时应同时检查竞态、GC 压力、调度抖动
和尾延迟。

不得为了“可能更快”而提前引入 `unsafe`、无界缓存、复杂无锁算法、难以验证的对象池或
多层抽象。性能结论应尽可能由 Benchmark、逃逸分析、Profile、Trace 或可重复的端到端
测试支持。性能和低延迟要求与代码精简、正确性、兼容性或可维护性发生明显冲突时，必须
列出方案、预期收益、复杂度和验证数据，按原则 1 与开发者确认后再实施。

### 23. 设计和外部调研前必须先复核既有结论

讨论、设计或调研一个功能前，必须先检查仓库中已经形成的设计文档、里程碑路线图、复核
清单、实施计划、相关代码和必要的 Git 历史，不能在没有核对既有结论的情况下重新提出
方案。复核结果至少要区分“已经确认”“尚未确认”“文档冲突”和“代码已经形成的事实”。

已经确认且仍适用的结论应直接继承，不重复讨论，也不能被后续临时建议静默改变。发现文档
之间、文档与代码之间存在冲突时，应先列出冲突来源、影响和建议处理方式，与开发者确认后
再修改设计。只有现有材料没有覆盖、已经失效或存在需要验证的疑问时，才进行外部资料调研；
外部项目的做法只能作为补充依据，不能覆盖 Origin 已确认的目标和约束。

完成新一轮讨论后，应把确认结果同步到对应的单一主设计文档，并修正引用该结论的旧文档，
避免同一主题长期保留多套相互矛盾的描述。

### 24. 待确认问题集中成较大批次讨论

设计、Review 或实现中存在多个需要开发者确认的问题时，不逐个零散询问。应先按依赖关系
和复杂度整理；存在 10 个或更多同层级待确认项时，每批至少集中确认 10 个问题。

1. 同一批中的问题应尽量属于同一个设计层级或相互关联的边界；
2. 每个问题都必须给出推荐方案、主要理由、复杂度和重要影响；
3. 已经确认且没有新冲突的结论直接继承，不为了凑数量重复询问；
4. 当前问题会阻塞后续问题时，先确认当前批次，再根据结论组织下一批；
5. 实际待确认问题不足 10 个时一次全部列出，不拆分或虚构没有价值的问题；
6. 方案数量以能够表达真实取舍为准，不机械提供没有实际价值的备选项。

开发者可以一次接受整批推荐，也可以只调整其中某项。确认结果应按原则 23 回写对应设计
文档，未确认项继续明确标记，不得提前混入实现。

### 25. 文档按目标发布版本隔离

已发布的定版资料必须放入 `docs/baseline/v{主版本}.{次版本}/`，例如已冻结的
`docs/baseline/v3.0/`；所有发布后的维护改动必须放入
`docs/maintenance/v{主版本}.{次版本}/`，例如 `docs/maintenance/v3.0/`、`v3.1/` 或
`v3.2/`。不得把后续功能、设计、计划、验收或使用说明追加到已经发布并冻结的基线目录；
基线只允许修复事实性错误、失效链接和排版问题。

新维护文档先按目标版本选择 `maintenance/` 下的目录，再按职责放置：未确认方案放入 `proposals/`，已确认设计放入
`design/`，实施计划放入 `plans/`，完成后的变更摘要放入 `changes/`，性能或验收材料放入
`reports/`，面向使用者的资料放入 `guides/`。仅在首次产生对应内容时创建子目录，不预先创建
空目录或空模板。

每篇维护文档开头必须写明状态、目标版本、兼容性和依赖基线；例如“基线：v3.0，目标：
v3.1.0”。一个版本发布后，将其定版资料收拢到 `baseline/` 并冻结；之后的修复和演进继续
进入相应 `maintenance/` 目录，新功能不得回填基线。
