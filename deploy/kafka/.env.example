# Apache 官方 JVM 镜像。固定修复版本，避免 latest 在下次启动时改变协议或默认值。
KAFKA_IMAGE=apache/kafka:4.3.1

# Ubuntu 在局域网中可被 Windows/其他客户端访问的地址。
KAFKA_ADVERTISED_HOST=192.168.8.3

# Ubuntu 暴露端口；修改时 advertised listener 会同步使用该值。
KAFKA_HOST_PORT=9092
