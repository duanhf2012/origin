# Binaries for programs and plugins
*.exe
*.exe~
*.dll
*.so
*.dylib

# Test binary, build with `go test -c`
*.test

# Output of the go coverage tool, specifically when used with LiteIDE
*.out
.DS_Store

# Default Linux output of examples/basic.
/basic

# Go build cache directory (local only, not for version control).
/.gocache/
